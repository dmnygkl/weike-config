
spring:
# profiles: dev
  cloud:
    config:
      server:
        bootstrap: true  # 表示服务器应该从自己的远程存储库配置自身
        git:
          uri: https://github.com/dmnygkl/weike-config.git
          username: dmnygkl
          password: dm900820
          default-label: master
  application:
    name: weike-cloud-config

server:
  port: 8888

